const initialState = {
  images: [],
  img_loading: false,
  img_loaded: false,
};

export default function (
  state = initialState,
  action
) {
  switch (action.type) {
    default:
      return state;
  }
}
